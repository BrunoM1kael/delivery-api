version: '3.8'

services:
  app-delivery:
    build: .
    container_name: delivery-api-container
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: always